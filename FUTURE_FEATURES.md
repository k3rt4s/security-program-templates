# Future features

Candidate documents for this repository, with the reason each is a candidate and the bar it would have to clear to be written.

This file exists so that candidates are recorded with their reasoning rather than rediscovered, and so that the ones deliberately not written stay deliberately not written. It does not restate what currently ships; the [Contents](README.md#contents) manifest does that and is kept current by script.

## Scored index

One bullet per scored candidate, scored with `ai_development/docs/board-scoring.md`.
This is the scored candidate history the scoring script reads; the reasoning for each stays under its own heading below.
Keep it current when a candidate is added, moved to Assessed and held, or shipped.

- **ir-plan-hierarchy** Assessed 2026-09-06: the table read shipped in [tabletop exercise design and after-action](incident-response/tabletop-exercise-pattern.md); the three-document IR hierarchy and DFIR-IRIS JSON case schema are [assessed and held](#assessed-and-held). `score: kind=feature gain=1/4/10 p=0.4 hours=0.5/1.5/3 ai=3 rev=two-way conf=assessed id=ir-plan-hierarchy` `return: likelihood about 4 in 10 that some part of this ships, from 16 of 16 assessed candidates shipping in some form since 2026-08-14 with 2 of those 16 shipping as a passage or under another name, and the policy-plan-runbook half being content this repository holds none of; impact if skipped, a future session re-reads the digest and re-derives the same verdict at 0.5 to 2 h, and the tabletop pattern keeps no documented review stage ahead of the exercise; evidence the Shipped list in this file (16 bullets) and its charter entry declining a charter as a genre, digest_infosec_age_of_ai_summit_2026.md lines 14 and 25, git log 2026-08-14 to 2026-09-01`
  - `worker: sonnet 2/3/5 h`
  - Moved to the data-root board 2026-09-06, group G1 order 2.

- **breach-cost-framing** Shipped 2026-09-06 as a passage under [Shipped](#shipped). Risk-reduction and cost-avoidance framing for the board update, and what an industry-average breach cost can and cannot support. `score: kind=feature gain=1/3/8 p=0.55 hours=0.25/0.5/1.5 ai=2 rev=two-way conf=opinion flags=external id=breach-cost-framing` `return: likelihood about 55 in 100 that this ships, as a passage rather than a document, from 2 of 16 shipped candidates landing as a passage in an existing file and this one failing the first test the same way the self-referential denominator did; impact if skipped, the board template keeps no position on borrowed loss figures and the metrics catalog keeps no gaming mode for them, 1 to 8 h of value to a reader who would otherwise put an industry average in front of a board as if it were their own loss distribution; evidence digest_threat_hunting_summit_2026.md line 24 and 38 for the figures and the frame, the self-referential denominator entry in this file for the passage precedent, board-reporting/quarterly-security-update-template.md read for the gap`
  - `worker: sonnet 1/1.5/3 h`
  - Moved to the data-root board 2026-09-06, group G3 order 1.

- **breach-response-anatomy** Shipped 2026-09-06 in a narrowed form under [Shipped](#shipped). Insurance limits, carrier notification and pre-identified vendor retainers, the half of the breach-response checklist the repository had not already placed. `score: kind=feature gain=1/4/10 p=0.35 hours=0.5/1/2 ai=3 rev=two-way conf=assessed id=breach-response-anatomy` `return: likelihood about 35 in 100 that a narrow version ships, because 4 of the checklist's 5 elements are already placed and only the insurance half is unclaimed; impact if skipped, the set keeps no record that a cyber policy can require carrier notice before a vendor is engaged, which is a constraint that binds the response and is invisible from inside the incident documents, 1 to 10 h to a reader who discovers it during an incident; evidence governance/decision-rights-register.md lines 87 to 91 already carrying preservation, legal hold, outside counsel and ransom authorization, the evidence readiness register entry in this file recording that the contact-list and IR-plan half was declined, digest_threat_hunting_summit_2026.md line 24`
  - `worker: sonnet 1.5/2.5/4 h`
  - Moved to the data-root board 2026-09-06, group G4 order 1.

- **external-authority-citation** Assessed and held 2026-09-06 under [Assessed and held](#assessed-and-held). The external-authority citation pattern for justifying a security initiative. `score: kind=docs gain=0.3/0.75/2 p=0.9 freq=1 hours=0.1/0.25/0.5 ai=1 rev=two-way conf=assessed id=external-authority-citation` `return: likelihood about once a year that a session re-opens this line, from 5 raw candidates sitting unassessed since 2026-08-21 and this file's own instruction to re-check anything still here in a year; impact 0.3 to 2 h of a session re-reading the digest and re-deriving that the cited report is unnamed at source and that a rhetorical technique is not an artifact a program produces; evidence the Ingested 2026-08-21 and Notes on vintage sections of this file, digest_rossouw_agentic_threat_hunting.md line 60 recording that the speaker did not name the report`
  - `worker: sonnet 0.5/1/2 h`
  - Moved to the data-root board 2026-09-06, group G2 order 1.

- **outcome-feedback-loop** A structured what-this-taught-us field on the postmortem and after-action, shipped 2026-09-06 under [Shipped](#shipped). `score: kind=feature gain=3/8/20 p=0.75 hours=0.75/1.5/3 ai=4 rev=two-way conf=assessed id=outcome-feedback-loop` `return: likelihood about 75 in 100 that this ships, from 16 of 16 assessed candidates shipping in some form and this one clearing all four tests on a gap read directly from the tree; impact if skipped, the postmortem keeps asking in prose whether an earlier signal was present but unalerted or simply not present and keeps no structured destination for the answer, so missing telemetry never reaches the evidence readiness register as a coverage row and never reaches the metrics catalog, 3 to 20 h of value across the incident set; evidence incident-response/blameless-postmortem-template.md line 108 asking the question and lines 112 to 125 whose action-item categories conflate a missing alert with missing data, incident-response/tabletop-exercise-pattern.md line 125 reusing that table, digest_rossouw_agentic_threat_hunting.md lines 19 and 33`
  - `worker: sonnet 2/3/5 h`
  - Moved to the data-root board 2026-09-06, group G1 order 1.

- **preauth-session-revocation-right** Candidate, assessed 2026-09-24, recorded under [Candidates](#candidates). A standing, ticket-free decision right to revoke a user's sessions and refresh tokens for incident containment. `score: kind=docs gain=1/3/8 p=0.6 hours=0.25/0.5/1 ai=1 rev=two-way conf=assessed id=preauth-session-revocation-right` `return: likelihood about 6 in 10 that a narrow version ships as a register row and a cross-reference passage, from every candidate that cleared the bar so far shipping in some form (20 shipped, 13 held at assessment) and this one clearing all four tests on a gap read directly from the tree rather than proposed from the digest; impact if skipped, the credential-and-session-compromise response pattern keeps naming session and refresh-token revocation as the control that actually stops the attacker while the register that assigns standing authority has no row for who may execute it without a change ticket, so the primary control in that document stays exposed to exactly the delay the digest's Q&A names as its most common post-incident lesson, 1 to 8 h of value to a reader who would otherwise re-derive the gap mid-incident or after a table read exposes it; evidence digest_johansen_azure_incident_readiness.md line 57 for the lesson, governance/decision-rights-register.md lines 80 to 101 for the starting set with no matching row and lines 99 and 101 for the closest existing rows (revoking a vendor's production credential, disabling an unowned non-human identity), identity/credential-and-session-compromise-response-pattern.md lines 26 to 28 for the control and line 101 for a skip-a-step row the pattern says the register holds but whose starting set does not list it`
  - `worker: sonnet 0.25/0.5/1 h`
  - Assessed 2026-09-24 from `digest_johansen_azure_incident_readiness.md`.

## The bar

A document belongs here only if it clears all four of these. Most candidate topics fail on the third.

1. **It is an artifact a security program produces**, not a checklist, a policy, a control library, or a tool evaluation. The repository is about the shape of documents that get written, reviewed, and argued over.
2. **It hands something to, or takes something from, the existing set.** The value of this collection is that it is a loop rather than a folder. A document with no edge into the others is a candidate for somewhere else.
3. **There is a defensible opinion to hold about it**, one that a reader could disagree with. Good content on the topic being abundant is not a reason to skip it, and being abundant and undifferentiated is not a reason to add to it. The test is whether there is a position worth taking that the freely available versions do not take.
4. **The opinion survives contact with a real organization.** A position that only works at a company with a mature program and a dedicated team is a blog post, not a template.

## Candidates

Topics that have cleared the bar above and are waiting to be written.

- **Pre-authorized session-revocation containment right** — candidate, assessed 2026-09-24 from `digest_johansen_azure_incident_readiness.md`. The talk's Q&A names its most common post-incident lesson as responders unable to revoke sessions or reset passwords without a ticket, and says this should be pre-authorized or automated (digest line 57). It clears the first test because the fix is a row in an existing artifact this repository already writes, not a new checklist: the [decision rights register](governance/decision-rights-register.md)'s starting set assigns standing authority for adjacent actions (revoking a vendor's production credential at line 99, disabling an unowned non-human identity at line 101) but has no row for a responder revoking a user's own sessions and refresh tokens as containment. It clears the second test because it plugs directly into two existing documents rather than sitting on its own: the register gains a row, and the [credential-and-session-compromise response pattern](identity/credential-and-session-compromise-response-pattern.md) gains a pointer where it currently names session and refresh-token revocation as "the step that actually stops the attacker" (lines 26 to 28) without saying who may execute it or that it should not wait on a ticket. It clears the third test on the position that this authority belongs in the register as a standing pre-authorization rather than approved per instance behind a change-management gate, because the interval a ticket adds is exactly the interval the credential pattern's own metric, time-to-session-revocation, measures as the number that matters (lines 88 to 95); a reader could reasonably disagree and argue the ticket trail is the control, not the obstacle. It clears the fourth test because the friction is generic IT service management, not an Azure feature, and the register and the response pattern already work at the scope of any organization holding either document. Scope for the write itself, when it is taken up: two decision-rights rows, both with the credential-and-session-compromise response pattern as criteria source, and a short passage in that pattern's "Where this connects" section pointing at them. The first is "Pre-authorize on-call responders to revoke a user's sessions and refresh tokens for containment without per-instance approval," held by one named person like every other row, because the register assigns decisions to people rather than roles (line 46, and failure mode 1 at line 126), and a row naming only who approves each revocation would move the ticket rather than remove it. The second is the skip-a-step row the pattern already says the register holds (pattern line 101) but whose starting set does not list it, a broken cross-reference the same write closes. The write should expect a reader to point out that identity platforms already scope revocation to an administrative role; the role grants the technical permission, and the register row is the organizational grant to use it without a ticket. No new document. Scored as `preauth-session-revocation-right` in the [Scored index](#scored-index).

## Assessed and held

Topics that were put through the bar and did not ship. The reasoning is recorded so the judgment is not repeated, and so a later session can see what would have to change for the verdict to change. Everything else that was assessed is in the shipped list below.

- **External-authority citation pattern** — held, assessed 2026-09-06. Fails the first test: it is a rhetorical technique for pitching a proposal, not an artifact a security program produces, reviews, and argues over. The self-referential denominator precedent shows that a first-test failure can still ship as a passage when an existing artifact has a specific gap. That passage route is not open here: [Section 8: Asks](board-reporting/quarterly-security-update-template.md#section-8-asks) already requires every ask to carry the decision, recommendation, cost of doing nothing, and what changes if it waits a quarter. External authority is not one of those four, and adding it would weaken the section because the ask must stand on the organization's own facts. It independently fails provenance: `digest_rossouw_agentic_threat_hunting.md` line 60 records that the speaker did not name the report, and a citation pattern whose worked example cannot be cited is not publishable professional signal. Reopen only with a named, retrievable report and a position on external authority that the board update's Asks section does not already hold better.

- **Three-document incident-response hierarchy: policy or charter, plan, and runbooks** — assessed and held 2026-09-06. It fails the first test because policies, plans, and runbooks are exactly the three genres this repository does not hold. The boundary is now confirmed by the security program charter shipping as a decision-rights register, the IR-plan half declining under the evidence readiness register, and the remediation SLA declining and reframing as exposure prioritization; a fourth candidate at the same boundary confirms it is real. Reconsider only if the developer explicitly widens repository scope to plans.

- **DFIR-IRIS JSON case schema** — assessed and held 2026-09-06. It fails the first test differently: it is a case-management tool data model whose value is portability between tools, rather than a position anyone could argue with. It also fails the third test, because there is nothing to disagree with in a field list. Reconsider only with a position on what a case record must capture that survives detachment from any one tool.

- **Azure responder-access model, six-step preparation list, log-extraction how-to** — assessed and held 2026-09-24, from `digest_johansen_azure_incident_readiness.md`. Three bundled points fail the first test the same way and because checklists and tool how-tos are outside this repository's scope: the responder-access model (Global Reader, Security Reader, Security Operator, PIM-eligible time-boxed roles, break-glass Global Admins on FIDO2, digest line 20), the six-step preparation list (digest line 55), and the log-extraction how-to (Invictus-IR Microsoft Extractor Suite paged by UPN or window, then parsed with SANS SOF-ELK, digest lines 44 to 46) are checklists and tool how-tos, not artifacts a security program produces, reviews, and argues over. They independently fail the fourth test as written, since all three name specific Azure/M365 roles and tools and generalizing them past Azure would mean discarding the only content that is specific. Bookmarking MSPortals.io and azdocswatch (digest line 22) and naming 1 to 3 internal Azure engineers before an incident (digest line 21) are the same category at a smaller scale and are held on the same reasoning without separate treatment. Reconsider only if the developer explicitly widens repository scope to tool guides or checklists.

- **Responder access provisioned and verified before an incident** — assessed and held 2026-09-24, already placed. Read narrowly this is the same Azure RBAC checklist as above and fails on the same grounds. Read as the general point, that granted access is a belief until someone has actually used it, it is already the position this repository takes: the [evidence readiness register](incident-response/evidence-readiness-register.md)'s "Test it by retrieving, not by reviewing" section states "an unexercised capability is a belief" and cites the [tabletop exercise pattern](incident-response/tabletop-exercise-pattern.md)'s "No magic capabilities" facilitation rule as the same rule applied elsewhere (evidence-readiness-register.md lines 133 to 139, tabletop-exercise-pattern.md line 80). A responder's PIM-eligible role that has never been activated in anger is the same belief the register already names for an untested evidence source and the exercise already names for an unproven restore. No new artifact closes a gap that is not open.

- **Log priority tiers and log centralization** — assessed and held 2026-09-24, already placed. The digest's critical/moderate/low log tiers (Entra sign-in and audit, UAL, Activity Log as critical; Graph activity, Defender for Identity, XDR as moderate; storage and workload logs as low, digest lines 23 to 27) and its comparison of Sentinel, Log Analytics, and Splunk/Elastic/OpenSearch for centralizing them are Azure-specific tooling content and fail the first and fourth tests the same way as the responder-access model above. The underlying position, that sources are not equally load-bearing and should be prioritized by which incident question they answer rather than by category, is already this repository's position: the [evidence readiness register](incident-response/evidence-readiness-register.md)'s four-questions table (lines 29 to 36) ranks sources by what they can prove, not by a fixed tier list, and is scoped to crown-jewel capabilities rather than an estate-wide log inventory (lines 17 to 21). A vendor-neutral version of the digest's tiering adds nothing the four-questions table does not already generalize.

- **Retention: 90-day UAL / six-month Activity Log floor, E3 versus E5 tiering** — assessed and held 2026-09-24, already placed. The [evidence readiness register](incident-response/evidence-readiness-register.md)'s "Retention is the whole document" section already takes the position the digest's Q&A reaches independently: that a retention window is a price point described as policy, and the number to compare it against is dwell time from your own postmortems, not a compliance minimum or a vendor default (evidence-readiness-register.md lines 40 to 48). The digest records the 90-day and roughly six-month numbers as the speaker's rule of thumb, not a Microsoft default (digest line 28), and this repository does not publish an unverified second-hand figure. Nothing in the licence-tier point (E3 vs E5 changing retention, no day counts given) survives as a figure worth citing; the position it would support is already stated without it.

- **Attack catalog: password spraying, MFA fatigue, illicit OAuth consent, exposed RDP/SSH, business-email-compromise phases, Kali365 pricing** — assessed and held 2026-09-24, from `digest_johansen_azure_incident_readiness.md` lines 32 to 39. Threat descriptions are not artifacts a security program produces, reviews, or argues over, and fail the first test as a block. The Kali365 price additionally fails provenance on its own terms, reported in the digest as "reportedly" about $500 to $600 (digest line 39), and is not published here. Two points inside this list are assessed separately below because they carry a position rather than a description: device-code phishing, and illicit OAuth consent as an AI-governance discovery signal.

- **Device-code phishing: session revocation over password reset** — assessed and held 2026-09-24, already placed. The digest's point that device-code phishing survives a password reset because the victim authenticated on a genuine Microsoft page, so sessions must be revoked instead (digest line 40), is the exact position the [identity](identity/README.md) set already holds and generalizes past any one phishing technique. The [credential-and-session-compromise response pattern](identity/credential-and-session-compromise-response-pattern.md) states it as the document's organizing argument: rotation is not the primary control, forced session and refresh-token revocation is, because an infostealer or a stolen session survives a password change untouched (credential-and-session-compromise-response-pattern.md lines 5 to 8 and 20 to 28, restated as the first common failure mode at line 108). Nothing in the digest's framing is narrower or better than the existing document's; it is one more trigger type the pattern's three-trigger field (breach-corpus hit, stealer-log notification, phishing report, line 54) already accommodates.

- **Response runbook: containment, eradication, recovery, lessons learned** — assessed and held 2026-09-24, from `digest_johansen_azure_incident_readiness.md` lines 50 to 53. Revoking Entra sessions, snapshotting VM disks, deny-all NSG rules, rotating Key Vault, deleting rogue Logic Apps and automation accounts, restoring from immutable Azure Backup, and refining Azure Policy are Azure-specific runbook and checklist content, and fail the first and fourth tests as written the same way the six-step list does. The one position embedded in it, that every secret should be presumed compromised and rotated after a broader compromise rather than assessed case by case, is a narrower instance of a position this repository already holds at the identity layer: the [credential-and-session-compromise response pattern](identity/credential-and-session-compromise-response-pattern.md)'s presumption rule, that what is presumed stolen is scoped to what a compromised host or identity could reach and the presumption resolves against the responder absent egress logging that proves narrower (lines 40 to 48), is the general form of "assume every secret is compromised." A Key-Vault-specific restatement of it would be the Azure checklist again, not a new position.

- **Copilot-plus-SharePoint tenant search returning passwords** — assessed and held 2026-09-24, from `digest_johansen_azure_incident_readiness.md` line 61. Held on two independent grounds. First, provenance: the digest attributes this to a pentester, not the speaker, during the pre-show discussion (digest line 61), and this repository does not publish an unverified second-hand claim as fact; this repository does not publish an unverified second-hand claim as a worked example regardless of whether it would otherwise clear the bar. Second, and independent of provenance, the failure mode it describes is already this repository's stated position: the [AI system register pattern](ai-governance/ai-system-register-pattern.md)'s "Data classification ceiling, enforced rather than asserted" section states that a row claiming a data limit that nothing enforces should record the highest class the system can actually reach, not the highest class it is supposed to reach, and names a filtered retrieval index and a redaction proxy as the kind of enforcement point a policy statement is not (ai-system-register-pattern.md lines 62 to 66, restated as failure mode 5 at line 152). A licensed Copilot inheriting whatever SharePoint permissions already exist is that exact gap.

- **Illicit OAuth consent grants** — assessed and held 2026-09-24, already placed. The digest names OAuth consent grants to malicious multi-tenant apps as an attack (digest line 33) and, in its own evaluation section, names OAuth consent grants held by AI apps and agents as a check worth running (digest line 77). Both are already this repository's second discovery channel for the AI and non-human-identity surface: the [AI system register pattern](ai-governance/ai-system-register-pattern.md)'s discovery channels name "Identity: OAuth grants and third-party app consents against the corporate identity provider" as the highest-yield channel for tools that read corporate data, on the reasoning that the grant is the access (ai-system-register-pattern.md lines 81 to 82). Nothing in the digest narrows or extends that channel.

- **Agentic tooling taking over correlation, no fully autonomous attacker activity yet** — assessed and held 2026-09-24, from `digest_johansen_azure_incident_readiness.md` lines 62 to 63. The speaker's forward-looking expectation about agentic tooling and MCP connectors, and his observation that he has seen automated but not autonomous attacker activity, are commentary and prediction rather than a position a document in this set could take or an artifact a program produces. Fails the first test outright; there is no candidate here to narrow.

## Ingested 2026-08-21, not yet assessed

Raw candidates from public talk and summit digests. None of these has been put through the bar above; a session that wants one should assess it first and move it to Candidates or Assessed and held with reasoning.

Nothing sits here at present.

## Notes on vintage

The demand signals behind these candidates were gathered in August 2026 from published survey and practitioner reporting. Signals age. Anything still sitting in this file in a year should be re-checked against current reporting before it is written, rather than written on the strength of a note left here.

## Shipped

Items are annotated here when they ship, rather than deleted, so the reasoning stays with the outcome.

- **Breach response insurance duties** — shipped in a reframed narrow form, [compliance/regulatory-applicability-register.md](compliance/regulatory-applicability-register.md), [governance/decision-rights-register.md](governance/decision-rights-register.md), and [third-party-risk/vendor-risk-tiering-pattern.md](third-party-risk/vendor-risk-tiering-pattern.md). Assessed 2026-09-06 from an anatomy-of-a-breach-response checklist candidate that bundled who to call, an incident response plan, a ransom decision, insurance limits and pre-identified vendors. Four elements were found already placed rather than rewritten: evidence preservation, legal hold, outside counsel and ransom authorization already sit in the decision rights register and evidence-readiness reasoning, while the IR-plan and contact-list half had already been declined as plan and call-tree content this repository does not hold. The remaining narrow point clears the bar because a cyber policy is a contract with incident duties, a notification clock, spend conditions and an approved-responder panel, all of which bind the response while often sitting outside the security program in finance paperwork. It now lands as a carrier-notice duty in the regulatory applicability register, two assigned decision rows in the decision rights register, and a vendor-tiering passage that treats carrier panels as critical vendors chosen elsewhere. No incident response plan, contact list, insurer, broker, policy product or specific notification window was added; coverage questions stay with the organization's own broker and counsel.

- **Table read before tabletop exercise** — shipped 2026-09-06 in [incident-response/tabletop-exercise-pattern.md](incident-response/tabletop-exercise-pattern.md). The table read is the cheaper review stage before an exercise: decision owners read the procedure aloud against a scenario, in sequence and without a clock or injects, so broken ownership, access assumptions, and ordering are fixed before the exercise spends its two-hour room on decisions under time pressure. It is deliberately not a substitute for the exercise, which applies the clock the table read removes.

- **Outcome feedback loop** — shipped as a postmortem section and evidence-readiness connection, [incident-response/blameless-postmortem-template.md](incident-response/blameless-postmortem-template.md) and [incident-response/evidence-readiness-register.md](incident-response/evidence-readiness-register.md). Assessed 2026-09-06 from a feedback-loop-to-metrics candidate and scoped to incidents rather than to a standalone hunt/incident outcome document. It clears the first test because the postmortem is already the artifact that records what an incident proved, and it clears the second because a missing-telemetry fault now downgrades Coverage confidence in the evidence readiness register instead of dying as a lesson-learned sentence. The position is that "improve detection" is decoration unless it names the kind of detection-system fault: missing telemetry, present but unalerted, or alerted and tuned out. Those three faults have different owners, costs and fixes, so the postmortem records them separately and gives each one a destination outside the narrative. The worked incident now shows both a present-but-unalerted authentication pattern and missing egress telemetry, which keeps the example grounded in the document's own facts rather than in a new scenario.

- **Credential and session compromise response** — shipped, [identity/](identity/README.md). Assessed and written 2026-08-31 from the Flare "How Information Stealers Work" talk (YouTube FmF8cjViSlI). Cleared the third test on the position that password rotation is close to theatre against an infostealer: the infected host surrendered the live session cookie and the browser-extension TOTP seed in the same pass as the password, so forced session and refresh-token revocation and MFA re-enrollment are the primary controls and rotation is downstream cleanup, and a browser-extension authenticator shares the exact storage the credential was lifted from and so is not a second factor. Cleared the second test by pointing the [evidence-readiness register](incident-response/evidence-readiness-register.md)'s presumption-resolves-against-you rule at an endpoint instead of a database: the full artifact population on the host is presumed stolen unless egress logging proves narrower, which is the condition where the presumption does the work because most stealer infections leave no forensic trail. Test 4 is met by scoping the record to a single confirmed exposure and one host, handling the personal or unmanaged device case explicitly since a reimage is not enforceable there, and measuring time-to-session-revocation rather than time-to-password-reset. It edges into the [access review pattern](identity/access-review-pattern.md) for which authenticator types are acceptable and what the compromised identity could reach, the materiality and evidence-readiness registers for population and what can be ruled out, the decision-rights register and exception record for who may skip a step in the fixed order, the crown jewel inventory and risk register for where the identity's access widens the presumed population, and the metrics catalog for the revocation-versus-rotation gap.

- **Control evidence acceptance register** — shipped, [compliance/control-evidence-acceptance-register.md](compliance/control-evidence-acceptance-register.md). Assessed and written 2026-08-29 from the Certified Information Security open assessment platform value check, using only fictional Ambervale data in anonymous samples and retaining none of the platform's proprietary methodology, mappings, scoring, prompts, evidence catalog, interface, or report format. The comparison exposed an assumption the repository already made but did not define: the [control mapping](compliance/control-framework-mapping-pattern.md) required a per-target evidence index, while nothing recorded whether an item was actually sufficient for a control assertion. The position that clears the third test is that requested and received are collection states, not assurance decisions. The register accepts one evidence item against one canonical control assertion for a stated scope and period, records Partial and its uncovered remainder explicitly, expires acceptance on the event that invalidates it, and derives target-framework views through the mapping rather than maintaining copies. Evidence that cannot support a target mapped as Full forces the mapping or outward claim to be downgraded. It connects the AI system register and other operating artifacts to the mapping, customer assurance package, risk register, and board update without duplicating the incident-focused evidence readiness register. Test 4 is met by a metadata-and-reference spreadsheet scoped first to controls behind active certifications, customer or public commitments, and crown-jewel claims; evidence remains in its governed system of record.
- **The self-referential denominator** — shipped as a passage rather than a document, [metrics/security-metrics-catalog.md](metrics/security-metrics-catalog.md). Assessed 2026-08-20 and deliberately not written as its own file, because it fails the first test: it is a method, not an artifact a program produces. The catalog currently states that the fix for a ratio is to publish the denominator next to it, every time. That fix does not work for the coverage metrics that matter most, and the gap is worth closing because the catalog's own remedy is what fails. An EDR coverage figure sourced from the EDR console is 100 percent by construction: a machine with no agent is not in the console to be counted, so the denominator is supplied by the system being measured. Publishing it changes nothing, because it was never wrong, it was self-referential. That is a different failure from the one the catalog describes, which is a denominator that shrinks, and it is invisible to the catalog's remedy rather than merely unaddressed by it. The position to take is that a coverage claim needs its denominator from an authority independent of the thing being measured, and that the way to get one is to differential several authorities that should agree: the directory, the console, the asset system, the address space. The repository already holds this answer once, scoped narrowly, in the [AI system register](ai-governance/ai-system-register-pattern.md)'s four discovery channels and its "discovery coverage, not row count" metric; what is missing is the generalization to the estate. It edges cleanly into the [crown jewel inventory](assets/crown-jewel-inventory-pattern.md), which is the denominator for the board's coverage indicators, and into the [evidence readiness register](incident-response/evidence-readiness-register.md)'s Coverage confidence column, which asks the same question of a log source. Raised from an endpoint-reconciliation tool built outside this repository, so test 4 is already answered: it costs two exported inventories and a join, which is the lowest implementation bar of anything here. Written 2026-08-20 as the section "When the denominator comes from the thing being measured", with a paired row in the pairing table and a failure mode of its own. The catalog's existing denominator advice is left standing rather than rewritten, since a shrinking denominator and a self-referential one are different faults and the first section's remedy is correct for the fault it describes.
- **Breach-cost framing** — shipped as a passage rather than a document, [board-reporting/quarterly-security-update-template.md](board-reporting/quarterly-security-update-template.md). Assessed 2026-09-06 and deliberately not written as its own file, because it fails the first test: risk-reduction and cost-avoidance framing is a method for an ask, not an artifact a program produces. Section 8 requires each ask to state the cost of doing nothing, but the template did not distinguish an organization's loss distribution from an industry population in which it is only one unweighted member. The position that clears the gap is that an industry average can bound an order of magnitude when the organization has no loss history, but it cannot be presented as the organization's cost of doing nothing; that claim belongs to the organization's own postmortems, aged exceptions, and crown-jewel tolerable-outage figures. It pairs with the [self-referential denominator](metrics/security-metrics-catalog.md#when-the-denominator-comes-from-the-thing-being-measured): both figures look like evidence and survive publishing their source, but describe the wrong population. The catalog now names the borrowed figure as the numerator-side twin and links back to the board guidance. The conference-supplied figures that raised the candidate are deliberately not reproduced, because they were not from a primary source. Written as the Section 8 passage and a board-update failure mode, with no breach-cost figure published.
- **Evidence readiness register** — shipped, [incident-response/](incident-response/README.md). Assessed 2026-08-17 from a practitioner panel where a breach attorney said the thing that decides most reporting obligations is that nothing was preserved, so nobody can say whether data was exfiltrated. Found by the assumed-but-never-defined method, like the crown jewel inventory before it: the materiality record rules out bulk exfiltration "on the basis of complete egress logging for the window", and that clause carries the whole determination while nothing in the set said how an organization would know it were true. Scoped deliberately at assessment. The candidate as raised also bundled counsel, forensics, and carrier contacts with the engagement sequence, which is incident-response-plan content and a contact list, and this repository holds neither. That half ships as three criteria rows pointed at the decision rights register, which had no row for triggering preservation, attaching a hold, or engaging outside counsel. The positions it takes are that retention is compared against dwell time from your own postmortems rather than against a compliance minimum, that an unanswerable question is not a neutral gap because a harm-presumption standard resolves it against you at the full population, that preservation fires on suspicion at declaration and is a different and cheaper act than a legal hold, and that collection rights are the one column that cannot be fixed during the incident.
- **AI system register** — shipped, [ai-governance/](ai-governance/README.md).
- **Materiality determination record** — shipped, [incident-response/](incident-response/README.md).
- **Security exception and risk acceptance record** — shipped, [risk-management/](risk-management/README.md).
- **Vendor risk tiering** — shipped, [third-party-risk/](third-party-risk/README.md).
- **Exposure prioritization** — shipped, [vulnerability-management/](vulnerability-management/README.md). Held originally as a remediation SLA, which failed the first test because an SLA is a standard and this repository holds none. Reopened 2026-08-15 and shipped as the reframe recorded here, rather than by changing the repository's scope statement. The reframe turned out to be the stronger document anyway: the useful content is the arithmetic nobody does out loud, comparing finding arrival against closure to establish whether prioritization allocates a shortfall or solves one, and it almost always allocates. Also that the tail below the cut line is a decision rather than an oversight, and that grouping findings by root cause is the only move that reduces arrival rather than closure.
- **Customer assurance package** — shipped, [customer-assurance/](customer-assurance/README.md). Held originally as a weak pass, on the grounds that the topic drags toward tooling comparison and the surrounding advice is saturated. Reopened 2026-08-15. Built around the one angle that made it a security artifact rather than a sales one: every answer is a durable commitment that constrains the exception process afterwards, so answers are generated from the control mapping and the other registers rather than authored under deal pressure, and a question with no source artifact is a finding rather than something to answer from memory.
- **Crown jewel inventory** — shipped, [assets/](assets/README.md). Not from the research pass. Found by walking the set for artifacts that are assumed but never defined, the same method that promoted the regulatory applicability register. Five documents leaned on a crown-jewel list, and two of them pointed at each other for it: the access review pattern sourced its populations from the risk register, while the risk register treated the list as an input it already had. Neither defined it. The positions it takes are that the unit is a business capability rather than a machine, that the selection test is tolerable outage rather than importance because importance is not decidable, and that crown jewels are usually a shared dependency one level below the system anyone would name.
- **Security program charter** — shipped as something else, [governance/decision-rights-register.md](governance/decision-rights-register.md). Held originally because its decision-rights framing appeared to duplicate the exception record's approval tiers. Checking the set showed a larger problem: decision rights are partially specified in eight documents, and two of them independently report finding an authority nobody held. The resolution is to split criteria from assignment. Each domain document keeps the criteria, meaning what constitutes the decision and at what threshold it escalates, because that is domain knowledge and is useless out of context. One document carries the assignment, because that is an organizational fact that changes when people change and must be consistent across domains. The decisive argument for a single assignment table is not tidiness: an authority nobody holds is an absence, and absences are invisible from inside any one document. That also renamed the candidate. A charter as a genre is mostly decoration; the part that clears the bar is the decision-rights register alone.
- **Access review and entitlement certification** — shipped, [identity/](identity/README.md). Cleared the third test on the position that the reviewer is usually the wrong person: a people manager cannot tell what an entitlement grants and pays a cost for revoking but none for approving, so approve-all is the rational response to a badly framed question.
- **Regulatory applicability register** — shipped, [compliance/](compliance/README.md). Cleared the second test decisively by closing a hole this repository had created: the materiality record instructs you to list every applicable notification clock without saying how you would know them.
- **Security metrics catalog** — shipped, [metrics/](metrics/README.md). The recorded concern was that it would become a longer version of the board update's indicator table. Resolved by splitting the two jobs: the catalog is the source and carries every metric's gaming mode and blind spot, and the board section is the selection problem, which is the harder half. The board template now links into the catalog instead of restating it.
- **Tabletop exercise design and after-action** — shipped, [incident-response/](incident-response/README.md). The open question recorded here before writing was whether the after-action duplicated the postmortem enough to be a section of it instead. Resolved as a separate document: the two differ in whether the facts were discovered or authored, and half of the exercise document is design work that has no postmortem analogue, since reality does the design. What they share is the destination, so the after-action reuses the postmortem's action-item table verbatim rather than inventing a second format. One format, two documents, one backlog.
